---
name: step-08-increment-session-count
description: 'Increment session_count in config.json — only here, never again at closeout.'
---

# Step 8: Increment `session_count`

**Progress: Step 8 of 8** — Final step of the session-start sequence

**Increment `session_count`** in `config.json`. Only here — never again at closeout.

## Done

The session-start sequence is complete. Deliver the briefing and conduct the rest of the
session per the **During the session** guidance in `references/workflow-briefing.md`.
